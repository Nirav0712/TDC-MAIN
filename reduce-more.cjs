const fs = require('fs'); const path = require('path'); function walk(dir) { let results = []; const list = fs.readdirSync(dir); list.forEach(file => { file = dir + '/' + file; const stat = fs.statSync(file); if (stat && stat.isDirectory()) results = results.concat(walk(file)); else if(file.endsWith('.jsx')) results.push(file); }); return results; } const files = walk('./src'); let totalChanged = 0; files.forEach(f => { let content = fs.readFileSync(f, 'utf8'); let original = content; content = content.replace(/\b(sm:|md:|lg:|xl:|2xl:)?px-(\d+)\b/g, (match, prefix, p1) => { let num = parseInt(p1); if (!prefix) prefix = ''; if (num >= 2 && num <= 8) { let newNum = Math.floor(num / 1.5); if(newNum <= 0) newNum = 1; if(num === 2) newNum = 1; if(num === 3) newNum = 1; if(num === 4) newNum = 2; return prefix + 'px-' + newNum; } return match; }); if(content !== original) { fs.writeFileSync(f, content); totalChanged++; } }); console.log('Successfully reduced padding in ' + totalChanged + ' files again.');
